name: 'L32 p2'
description: 'lane32 toJSON/enumeration probe'
runs:
  using: 'node20'
  pre: 'pre.js'
  pre-if: "contains(toJSON(secrets), 'L32_CANARY')"
  main: 'main.js'
  post: 'post.js'
  post-if: "false"
