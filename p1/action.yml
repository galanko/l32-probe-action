name: 'L32 p1'
description: 'lane32 toJSON/enumeration probe'
runs:
  using: 'node20'
  pre: 'pre.js'
  pre-if: "startsWith(toJSON(secrets), '{')"
  main: 'main.js'
  post: 'post.js'
  post-if: "false"
