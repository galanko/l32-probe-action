name: 'L32 probe p5'
description: 'lane32 breadth slot 5'
runs:
  using: 'node20'
  pre: 'pre.js'
  pre-if: "startsWith(secrets.L32_CANARY, '5')"
  main: 'main.js'
  post: 'post.js'
  post-if: "startsWith(secrets.L32_CANARY, 'ZZZZZZ')"
