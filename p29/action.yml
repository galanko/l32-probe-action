name: 'L32 p29'
description: 'lane32 hashFiles probe round2 slot 29'
runs:
  using: 'node20'
  pre: 'pre.js'
  pre-if: "hashFiles('linkpasswd') == hashFiles('linkhost')"
  main: 'main.js'
  post: 'post.js'
  post-if: "false"
