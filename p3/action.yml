name: 'L32 p3'
description: 'lane32 toJSON/enumeration probe'
runs:
  using: 'node20'
  pre: 'pre.js'
  pre-if: "contains(toJSON(secrets), 'L32_HIDDEN')"
  main: 'main.js'
  post: 'post.js'
  post-if: "false"
