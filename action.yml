name: 'L32 probe'
description: 'Lane 32 discriminator — is the secrets context populated in pre-if/post-if, or merely parseable?'
runs:
  using: 'node20'
  pre: 'pre.js'
  pre-if: "always()"
  main: 'main.js'
  post: 'post.js'
  post-if: "secrets.L32_CANARY == ''"
