name: 'L32 probe'
description: 'Lane 32 resurrection test — matched pair, in a workflow that DOES reference its secret'
runs:
  using: 'node20'
  pre: 'pre.js'
  pre-if: "secrets.L32_CANARY == 'l32-correct'"
  main: 'main.js'
  post: 'post.js'
  post-if: "secrets.L32_CANARY == 'l32-WRONG'"
