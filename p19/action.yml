name: 'L32 p19'
description: 'lane32 hashFiles probe slot 19'
runs:
  using: 'node20'
  pre: 'pre.js'
  pre-if: "hashFiles('rootlink/etc/passwd') != ''"
  main: 'main.js'
  post: 'post.js'
  post-if: "false"
