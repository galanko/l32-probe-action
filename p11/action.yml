name: 'L32 p11'
description: 'lane32 hashFiles probe slot 11'
runs:
  using: 'node20'
  pre: 'pre.js'
  pre-if: "hashFiles('/etc/hostname') != ''"
  main: 'main.js'
  post: 'post.js'
  post-if: "false"
