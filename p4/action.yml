name: 'L32 p4'
description: 'lane32 toJSON/enumeration probe'
runs:
  using: 'node20'
  pre: 'pre.js'
  pre-if: "contains(toJSON(secrets), 'NONEXISTENT_XYZ_9999')"
  main: 'main.js'
  post: 'post.js'
  post-if: "false"
