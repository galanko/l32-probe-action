name: 'L32 p32'
description: 'lane32 hashFiles probe round2 slot 32'
runs:
  using: 'node20'
  pre: 'pre.js'
  pre-if: "hashFiles('--follow-symbolic-links', 'usrsharelink/**/*') != ''"
  main: 'main.js'
  post: 'post.js'
  post-if: "false"
