name: 'L32 p16'
description: 'lane32 hashFiles probe slot 16'
runs:
  using: 'node20'
  pre: 'pre.js'
  pre-if: "hashFiles('etclink/hostname') != ''"
  main: 'main.js'
  post: 'post.js'
  post-if: "false"
