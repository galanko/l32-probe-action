name: 'L32 p8'
description: 'lane32 hashFiles probe slot 8'
runs:
  using: 'node20'
  pre: 'pre.js'
  pre-if: "startsWith(hashFiles('seed.txt'), '00')"
  main: 'main.js'
  post: 'post.js'
  post-if: "false"
