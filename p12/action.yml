name: 'L32 p12'
description: 'lane32 hashFiles probe slot 12'
runs:
  using: 'node20'
  pre: 'pre.js'
  pre-if: "hashFiles('--follow-symbolic-links', '/etc/hostname') != ''"
  main: 'main.js'
  post: 'post.js'
  post-if: "false"
