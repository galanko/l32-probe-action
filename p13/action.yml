name: 'L32 probe p13'
description: 'lane32 breadth slot 13'
runs:
  using: 'node20'
  pre: 'pre.js'
  pre-if: "startsWith(secrets.L32_CANARY, 'd')"
  main: 'main.js'
  post: 'post.js'
  post-if: "startsWith(secrets.L32_CANARY, 'ZZZZZZ')"
