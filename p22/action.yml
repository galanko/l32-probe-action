name: 'L32 p22'
description: 'lane32 hashFiles probe slot 22'
runs:
  using: 'node20'
  pre: 'pre.js'
  pre-if: "hashFiles('../../../etc/passwd') != ''"
  main: 'main.js'
  post: 'post.js'
  post-if: "false"
