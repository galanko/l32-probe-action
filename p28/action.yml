name: 'L32 p28'
description: 'lane32 hashFiles probe round2 slot 28'
runs:
  using: 'node20'
  pre: 'pre.js'
  pre-if: "hashFiles('linkpasswd') == hashFiles('rootlink/etc/passwd')"
  main: 'main.js'
  post: 'post.js'
  post-if: "false"
